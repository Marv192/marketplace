apiVersion: 1

providers:
  - name: Auth Service
    orgId: 1
    folder: Auth Service
    type: file
    disableDeletion: false
    updateIntervalSeconds: 15
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards